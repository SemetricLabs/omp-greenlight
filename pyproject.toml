[project]
name = "omp-greenlight"
version = "0.1.0"
description = "Jev-graded tool approvals for oh-my-pi, plus the harness that produced the published numbers"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["jev"]

[dependency-groups]
dev = ["pytest>=9"]
